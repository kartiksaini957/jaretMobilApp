import 'package:flutter/material.dart';

/// Standalone mint/teal palette for the Financial Overview screen —
/// its own family, distinct from the rest of the app.
class FinancialColors {
  FinancialColors._();

  static const bgTop = Color(0xFF1FE0D2);
  static const bgBottom = Color(0xFF0C8F86);

  static const white = Colors.white;
  static const mutedText = Color(0xCCFFFFFF);
  static const faintText = Color(0x99FFFFFF);

  static const cardLightFill = Color(0x33FFFFFF);
  static const cardDarkFill = Color(0x330B4A44);
  static const cardBorder = Color(0x33FFFFFF);

  static const statusGood = Color(0xFF86EFAC);
  static const statusNeutral = Color(0xFFFFC24B);
  static const statusBad = Color(0xFFFF7A59);

  static const alertBorder = Color(0xFFFF6B6B);
  static const goodText = Color(0xFF7CFFB2);

  static const donutColors = [
    Color(0xFF9FF3E6),
    Color(0xFF5FE0D0),
    Color(0xFF3AC7BC),
    Color(0xFF269E96),
    Color(0xFF176E68),
  ];
}
