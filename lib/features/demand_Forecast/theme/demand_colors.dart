import 'package:flutter/material.dart';

/// Standalone teal palette for the Demand Forecast screens — deliberately
/// distinct from the rest of the app's blue "aurora glass" system, to
/// match the reference design for this feature.
class DemandColors {
  DemandColors._();

  static const bgTop = Color(0xFF22D6C6);
  static const bgBottom = Color(0xFF0D7C74);

  static const white = Colors.white;
  static const mutedText = Color(0xCCFFFFFF);
  static const faintText = Color(0x99FFFFFF);

  static const cardFill = Color(0x26064840);
  static const cardBorder = Color(0x33FFFFFF);

  static const pillFill = Color(0xFF33534A);
  static const badgeNeutral = Color(0xFF6FDB6C);
  static const darkInset = Color(0xFF0B4A44);

  static const actionButton = Color(0xFF17BBA8);
}
