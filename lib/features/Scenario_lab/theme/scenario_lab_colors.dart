import 'package:flutter/material.dart';

/// Standalone bright cyan/blue "glow" palette for the Scenario Lab
/// (what-if scenario) screen.
class ScenarioLabColors {
  ScenarioLabColors._();

  static const bgTop = Color(0xFF1E88E5);
  static const bgBottom = Color(0xFF0A2A57);
  static const glow = Color(0xFF4FC3F7);

  static const white = Colors.white;
  static const mutedText = Color(0xCCFFFFFF);
  static const faintText = Color(0x99FFFFFF);

  static const cardFill = Color(0x1FFFFFFF);
  static const cardBorder = Color(0x33FFFFFF);
  static const cardFillStrong = Color(0x2EFFFFFF);

  static const statusGood = Color(0xFF6FDB6C);
  static const statusWarn = Color(0xFFFFC24B);
  static const statusBad = Color(0xFFFF6B6B);
  static const statusInfo = Color(0xFF4FC3F7);
}
